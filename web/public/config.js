// Overwritten by `bun run start:web` from env (VITE_API_URL / VITE_WS_URL).
// Safe default: empty → client uses same-origin or build-time Vite defines.
window.__NANOCORE_CONFIG__ = window.__NANOCORE_CONFIG__ || {};
