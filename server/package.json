{
  "name": "server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "echo \"server runs from source with bun\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tldraw/sync-core": "^3.15.1",
    "hono": "^4.7.10",
    "tldraw": "^3.15.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.3"
  }
}
